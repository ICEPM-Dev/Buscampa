/**
 * Botón de Google para login/registro.
 * Componente simple sin props complejas.
 */
import GoogleButton from "./GoogleButton";

export default function GoogleLoginButton() {
  return <GoogleButton text="Google" fullWidth />;
}
